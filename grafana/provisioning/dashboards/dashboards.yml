apiVersion: 1

providers:
  - name: tsroute
    orgId: 1
    folder: tsroute
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
